import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class handMin here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class handMin extends clock
{
    /**
     * Act - do whatever the handMin wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        setRotation(clock.degreeM);
        if (book.isBookOpen == 1 || book.isBookOpen == 2)
        {
            getWorld().removeObject(this);
        }
    }    
}
